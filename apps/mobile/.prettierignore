# Prettier 不碰的东西。判据是「这份文件是不是产物」，不是「它有没有进 git」。
#
# 放在 apps/mobile/ 里而不是仓库根：这份配置只属于 iOS 客户端。上游的 web/desktop/
# 服务端各有自己的格式化约定，整仓一份 Prettier 配置会把它们一起改掉。

# Expo prebuild 的产物，从不手改（见 memoh-ios-dev.md）。
ios/
.expo/
expo-env.d.ts

# 验收产物：截图/录屏/构建中间物、Maestro 的视图树 dump。它们按轮次显式 `git add -f`
# 进库，改了就不再是当时那一棵树——而 `pnpm ios:format` 会把它们整份重排。
verification/.artifacts/
verification/**/out/

# 点系统授权框上的「Allow」。
#
# 为什么是坐标：系统框由 SpringBoard 画，不进 App 的无障碍树（同 `04-tap-allow.yaml` 的
# 动作按钮）。坐标从 `02-permission-alert.png` 量出来，写在 flow 里而不是算出来——
# "点在哪个像素"正是这一步要显式说明的东西。
appId: ai.memoh.ios
---
- tapOn:
    point: '69%,59%'
- waitForAnimationToEnd:
    timeout: 5000
